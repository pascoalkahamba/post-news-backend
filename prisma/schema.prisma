// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model User {
  id             Int          @id @default(autoincrement())
  email          String       @unique
  password       String       @unique
  username       String
  cellPhone      String       @unique
  resetToken     String?      @unique
  resetExpiresAt DateTime?
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  role           Role         @default(USER)
  posts          Post[]
  reactions      Reaction[]
  profile        Profile?
  friendships    Friendship[]
  comments       Comment[]
  replies        Reply[]
  favorites      Favorite[]
}

model Profile {
  id      Int      @id @default(autoincrement())
  bio     String
  picture Picture?
  user    User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  userId  Int      @unique
}

model Category {
  id          Int    @id @default(autoincrement())
  name        String
  description String
  posts       Post[]
}

model Post {
  id         Int        @id @default(autoincrement())
  createdAt  DateTime   @default(now())
  updatedAt  DateTime   @updatedAt
  title      String     @db.VarChar(255)
  picture    Picture?
  comments   Comment[]
  reactions  Reaction[]
  favorites  Favorite[]
  content    String     @db.LongText
  published  Boolean    @default(false)
  category   Category   @relation(fields: [categoryId], references: [id], onDelete: Cascade)
  author     User       @relation(fields: [authorId], references: [id], onDelete: Cascade)
  authorId   Int
  categoryId Int
}

model Friendship {
  id           Int      @id @default(autoincrement())
  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  userId       Int
  dateOfFriend DateTime @db.Date
}

model Comment {
  id        Int        @id @default(autoincrement())
  content   String     @db.LongText
  postId    Int
  userId    Int
  user      User       @relation(fields: [userId], references: [id], onDelete: Cascade, map: "Comment_user_fkey")
  post      Post       @relation(fields: [postId], references: [id], onDelete: Cascade, map: "Comment_post_fkey")
  replies   Reply[]
  reactions Reaction[]
  createdAt DateTime   @default(now())
}

model Reply {
  id        Int        @id @default(autoincrement())
  content   String     @db.LongText
  commentId Int
  userId    Int
  user      User       @relation(fields: [userId], references: [id], onDelete: Cascade, map: "Reply_user_fkey")
  comment   Comment    @relation(fields: [commentId], references: [id], onDelete: Cascade, map: "Reply_comment_fkey")
  reactions Reaction[]
  createdAt DateTime   @default(now())
}

model Reaction {
  id     Int @id @default(autoincrement())
  userId Int

  postId    Int?
  commentId Int?
  replyId   Int?

  type      ReactionType
  createdAt DateTime     @default(now())

  user    User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  post    Post?    @relation(fields: [postId], references: [id], onDelete: Cascade)
  comment Comment? @relation(fields: [commentId], references: [id], onDelete: Cascade)
  reply   Reply?   @relation(fields: [replyId], references: [id], onDelete: Cascade)

  @@unique([userId, postId])
  @@unique([userId, commentId])
  @@unique([userId, replyId])
}

model Favorite {
  id        Int      @id @default(autoincrement())
  userId    Int
  postId    Int
  createdAt DateTime @default(now())

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)
  post Post @relation(fields: [postId], references: [id], onDelete: Cascade)

  @@unique([userId, postId])
}

model Picture {
  id        Int      @id @default(autoincrement())
  url       String   @db.LongText
  name      String
  post      Post?    @relation(fields: [postId], references: [id], onDelete: Cascade)
  profile   Profile? @relation(fields: [profileId], references: [id], onDelete: Cascade)
  profileId Int?     @unique
  postId    Int?     @unique
}

enum Role {
  USER
  ADMIN
}

enum ReactionType {
  LIKE
  DISLIKE
}

model VerificationCode {
  id        Int      @id @default(autoincrement())
  email     String
  code      String
  operation String
  expiresAt DateTime
  createdAt DateTime @default(now())
}
